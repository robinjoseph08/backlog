## Development

Use `mise` instead of `make` for project tasks.

## Agent skills

### Issue tracker

Issues are tracked in GitHub at `robinjoseph08/backlog`. See `docs/agents/issue-tracker.md`.

### Triage labels

The repo uses the five default triage labels plus `spec` for issues processed by `to-spec`. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. See `docs/agents/domain.md`.
